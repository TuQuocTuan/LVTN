info:
  name: Lấy ds món hôm nay
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5000/api/dashboard/dish-today
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
