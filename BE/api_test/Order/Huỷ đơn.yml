info:
  name: Huỷ đơn
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:5000/api/orders/cancelOrder
  body:
    type: json
    data: |-
      {
        "order_id":247,
        "order_detail_id":336
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
