info:
  name: Lấy session theo bàn
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5000/api/tables/getsession
  body:
    type: json
    data: |-
      {
        "table_id":1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
