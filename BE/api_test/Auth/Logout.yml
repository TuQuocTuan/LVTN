info:
  name: Logout
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5000/api/auth/logout
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
