info:
  name: Tìm công thức
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:5000/api/recipes/search?name=chỉ
  params:
    - name: name
      value: chỉ
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
